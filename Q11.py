# program to create a tuple
point = (3, 4) #Tuple
# accessing elements by index
print(point[0]) # Output: 3
# Tuples are immutable so we cannot modify the data once it is created 
point[0] = 5 # This statement will result in TypeError: 'tuple' object does not support item assignment